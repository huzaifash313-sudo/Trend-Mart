/* TrendsMart — static brand marketing media (public/media) */

/**
 * Homepage brand promo reel — muted autoplay loop.
 * `?v=8` busts caches. Pure hue-mapped so only the seagreen/teal pixels move
 * to Rich Wine Maroon (~344°) — white, black, shape & glow untouched. Matches
 * the app's default Rich Wine Maroon brand (primary #B72B51 / hero #711733).
 * Bump again only if the asset is ever replaced.
 */
export const BRAND_PROMO_VIDEO = "/media/brand/promo-reel.mp4?v=8";
