/* TrendsMart — static brand marketing media (public/media) */

/**
 * Homepage brand promo reel — muted autoplay loop.
 * `?v=2` busts browser/proxy caches: v1 shipped the old teal/green-branded
 * reel, v2 is the deep-plum regrade (hue-rotated + tuned) that matches the
 * current brand. Bump again if the asset is ever re-graded.
 */
export const BRAND_PROMO_VIDEO = "/media/brand/promo-reel.mp4?v=2";
